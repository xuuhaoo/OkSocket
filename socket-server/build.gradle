apply from: '../lib_configuration.gradle'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile project(':common-interface')
    compile 'com.android.support:support-annotations:26.1.0'
}
